# LightReach / Palmetto credentials
# (only needed if session.json is missing/expired)
LIGHTREACH_EMAIL=
LIGHTREACH_PASSWORD=

# Google Sheets target
FUNDING_REPORT_SHEET_ID=     # the long ID from the sheet URL
GOOGLE_SERVICE_ACCOUNT_JSON= # paste the FULL JSON content here, OR a path to a .json file

# Paths (defaults usually fine)
SESSION_PATH=./session.json
RAW_OUTPUT_DIR=./raw_audits
